/**
 * multiplication of number
 */
var num=2;
//creating function to perform multiplication
var mul=function(n){
    for (let index = 0; index <= 10; index++) {
        console.log(n+' * '+index+' = '+(n*index));
        
    }
}
mul(num);
 